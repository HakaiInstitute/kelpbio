# samples errors on an object that is not a fit

    Code
      samples(1)
    Condition
      Error in `samples()`:
      ! `fit` must be a <kb_fit> object.
      i Create one with a `kb_fit_*()` fitting function.

