% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kb_check_data_weight_nereo.R
\name{kb_check_data_weight_nereo}
\alias{kb_check_data_weight_nereo}
\title{Validate Nereocystis Weight Model Input Data}
\usage{
kb_check_data_weight_nereo(data, x_name = deparse(substitute(data)))
}
\arguments{
\item{data}{A data frame of weight observations (see
\code{\link[=kb_check_data_weight_nereo]{kb_check_data_weight_nereo()}} for the required columns).}

\item{x_name}{A string naming \code{data} in error messages.}
}
\value{
\code{data}, invisibly; called for its side effect of validating \code{data}.
}
\description{
Check that \code{data} contains the columns required to fit the \emph{Nereocystis
luetkeana} weight model, with appropriate types and values, erroring if it
does not.
}
\details{
Required columns: numeric \code{diameter} (> 0), numeric \code{weight} (> 0), and factor
or character \code{site} and \code{year}, with no missing values.

Units are the user's choice: the model centres log-diameter at the geometric
mean of the observed diameter, so the diameter unit does not affect the fit or
predictions, and weight is returned in whatever unit it was supplied in. The
only requirement is that prediction data use the same units as the fitted data.
}
\examples{
data <- data.frame(
  diameter = c(20, 35), weight = c(0.5, 2.1),
  site = factor(c("a", "b")), year = factor(c("2020", "2021"))
)
kb_check_data_weight_nereo(data)
}
\seealso{
Other data:
\code{\link{data_weight_sim_nereo}},
\code{\link{fit_weight_sim_nereo}}
}
\concept{data}
