% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kb_predict_weight.R
\name{kb_predict_weight}
\alias{kb_predict_weight}
\title{Predict Allometric Weight}
\usage{
kb_predict_weight(
  fit,
  new_data = NULL,
  by = NULL,
  uncertainty = c("marginal", "typical"),
  conf_level = 0.95,
  estimate = stats::median,
  sig_fig = 3
)
}
\arguments{
\item{fit}{A \code{kb_fit_weight} object.}

\item{new_data}{A data frame with a \code{diameter} column (and the \code{by} columns),
or \code{NULL} to auto-generate a diameter sequence over the observed range.}

\item{by}{A character vector of grouping factors to predict by, or \code{NULL}
for the population-level prediction.}

\item{uncertainty}{A string, one of \code{"marginal"} or \code{"typical"}, controlling
how random-effect factors not named in \code{by} are treated.}

\item{conf_level}{A number between 0 and 1 giving the compatibility-interval
level.}

\item{estimate}{A function giving the point estimate (e.g. \code{median}).}

\item{sig_fig}{A whole number of significant figures for summary output.}
}
\value{
A \code{kb_predictions} object: a summary tibble with \code{estimate}, \code{lower},
\code{upper} plus the grouping columns.
}
\description{
Predict weight over a diameter sequence from a fitted weight model. The
summary is the summariser over \code{posterior_epred()} (the prediction engine).
For the raw posterior prediction draws use \code{posterior_epred()} /
\code{posterior_predict()} directly.
}
\details{
\code{by} selects grouping factors that each get their own curve, held at their
observed estimated random effects. \code{uncertainty} controls factors not named
in \code{by}: \code{"marginal"} (a new, unobserved level: drawn from the estimated
hyperprior) or \code{"typical"} (the population-average: random effects zeroed).
For the weight model the available \code{by} values are \code{NULL}, \code{"site"}, and
\code{c("site", "year")}.
}
