% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kb_fit_weight_nereo.R
\name{kb_fit_weight_nereo}
\alias{kb_fit_weight_nereo}
\title{Fit the Nereocystis Weight Model}
\usage{
kb_fit_weight_nereo(
  data,
  priors = NULL,
  prior_only = FALSE,
  chains = 4L,
  niters = 1000L,
  nthin = 1L,
  cores = NULL,
  seed = NULL,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{data}{A data frame of observations.}

\item{priors}{A named list of prior objects (see \code{\link[=kb_priors_weight_nereo]{kb_priors_weight_nereo()}}), or
\code{NULL} to use the defaults. Supplied entries override the corresponding
defaults; unspecified entries keep their defaults.}

\item{prior_only}{A flag specifying whether to sample from the priors only
(the likelihood is switched off), for prior predictive checks.}

\item{chains}{A whole number of MCMC chains.}

\item{niters}{A whole number of saved post-warmup draws per chain (warmup
defaults to match).}

\item{nthin}{A whole number giving the thinning interval.}

\item{cores}{A whole number of cores for parallel chains, or \code{NULL} to use
\code{getOption("mc.cores")} (falling back to \code{chains}), capped at the available
cores.}

\item{seed}{A whole number passed to \code{\link[rstan:stanmodel-method-sampling]{rstan::sampling()}} to make the fit
reproducible, or \code{NULL} (the default). When \code{NULL}, rstan draws its own seed
from R's RNG, so a preceding \code{set.seed()} also makes the fit reproducible; an
explicit \code{seed} takes precedence over the RNG state.}

\item{quiet}{A flag specifying whether to suppress console output. When
\code{FALSE} (the default), rstan's sampling progress and warnings are printed;
when \code{TRUE}, all sampler output is suppressed.}

\item{...}{Additional arguments passed to \code{\link[rstan:stanmodel-method-sampling]{rstan::sampling()}}, including a
\code{control} list (merged over the \code{adapt_delta = 0.95} default).}
}
\value{
An object of class \code{c("kb_fit_weight", "kb_fit")}.
}
\description{
Fit the full allometric weight model for \emph{Nereocystis luetkeana} (quadratic
log-diameter mean with site intercept, site slope, and site:year random
effects, Student-t likelihood) to weight data via Stan.
}
\details{
\code{niters} is the number of saved post-warmup draws per chain; warmup defaults
to match \code{niters} and the post-warmup phase is thinned by \code{nthin}. The live
\code{stanfit} is discarded after fitting: the returned object stores the extracted
posterior draws (including the \code{log_lik} and \code{yrep} generated quantities),
diagnostics, data, and metadata.

With the default \code{quiet = FALSE} the sampler's progress and warnings are
printed to the console; \code{quiet = TRUE} suppresses all sampler output. Inspect
convergence with \code{\link[=converged]{converged()}} / \code{\link[=glance]{glance()}} / \code{\link[=summary]{summary()}}.

Chains run in parallel by default (\code{cores = NULL} uses \code{getOption("mc.cores")},
falling back to \code{chains}, capped at the available cores). Set
\code{options(mc.cores = 1)} (or pass \code{cores = 1}) on shared servers, in
containers, or inside another parallel context. On Windows, parallel chains
run in separate processes that load the installed package, so use
\code{cores = 1} when running from \code{devtools::load_all()}.

The sampler runs with \code{adapt_delta = 0.95} (a smaller leapfrog step than
Stan's 0.8 default, suited to the hierarchical geometry and reducing
divergences). Override it, or set any other sampler control, by passing a
\code{control} list through \code{...}, e.g.
\code{kb_fit_weight_nereo(data, control = list(adapt_delta = 0.99))}; only the
entries supplied are changed.

The site:year random effect is determined from the data rather than chosen: it
is included whenever the data span more than one year, and omitted when they do
not (the interaction is then confounded with the site effect). When years are
present but no site was sampled in more than one year, the effect is retained
but the site vs site:year split is not identifiable, and a warning is issued.
}
\examples{
if (interactive()) {
  fit <- kb_fit_weight_nereo(data_weight_sim_nereo)
  tidy(fit)
}
# A pre-fit example model ships with the package:
tidy(fit_weight_sim_nereo)
}
\concept{model}
