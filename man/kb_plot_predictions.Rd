% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kb_plot_predictions.R
\name{kb_plot_predictions}
\alias{kb_plot_predictions}
\title{Plot Model Predictions}
\usage{
kb_plot_predictions(
  predictions,
  x = NULL,
  style = NULL,
  facet = NULL,
  observed = NULL,
  max_facets = 12L,
  ...
)
}
\arguments{
\item{predictions}{A \code{kb_predictions} object.}

\item{x}{The predictor column name; \code{NULL} infers it from the metadata.}

\item{style}{One of \code{"ribbon"} (continuous predictor) or \code{"pointrange"};
\code{NULL} infers it from the predictor type.}

\item{facet}{Grouping variables to facet by; \code{NULL} infers them from the
metadata.}

\item{observed}{Optional raw data to overlay as points; \code{NULL} for none.}

\item{max_facets}{A whole number capping the facet panels drawn; if the
grouping has more groups, the first \code{max_facets} are shown with a warning.
Use \code{Inf} to disable.}

\item{...}{Unused.}
}
\value{
A \code{ggplot} object.
}
\description{
Render a \code{ggplot} from a \code{kb_predictions} object (the output of a
\verb{kb_predict_*()} function). Operates on prediction data frames in a pipe-based
workflow; it never takes a fit object. The result is a \code{ggplot} the user can
extend with \code{+}.
}
\details{
\code{x}, \code{style}, and \code{facet} default to \code{NULL} and are inferred from the
prediction's metadata (the predictor column, predictor type, and grouping
variables); each remains an overridable argument. If the metadata has been
stripped (e.g. by dplyr post-processing) and \code{x} cannot be inferred, the
function errors and asks for \code{x}.
}
\seealso{
Other prediction: 
\code{\link{autoplot.kb_predictions}()},
\code{\link{kb_predict_weight}()},
\code{\link{kb_predict_weight_by}()}
}
\concept{prediction}
