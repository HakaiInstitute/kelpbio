% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kb_plot_predictions.R
\name{kb_plot_predictions}
\alias{kb_plot_predictions}
\title{Plot Model Predictions}
\usage{
kb_plot_predictions(
  predictions,
  ...,
  x = NULL,
  observed = NULL,
  max_facets = 12L
)
}
\arguments{
\item{predictions}{A \code{kb_predictions} object.}

\item{...}{Unused.}

\item{x}{A string naming the x-axis column, or \code{NULL} to infer it from the
metadata.}

\item{observed}{A data frame of raw observations to overlay as points, or
\code{NULL} for none.}

\item{max_facets}{A whole number capping the facet panels drawn; if the
grouping has more groups, the first \code{max_facets} are shown with a warning.
Use \code{Inf} to disable.}
}
\value{
A \code{ggplot} object.
}
\description{
Render a \code{ggplot} from a \code{kb_predictions} object (the output of a
\verb{kb_predict_*()} function).
}
\details{
\code{x} defaults to \code{NULL} and is inferred from the prediction's metadata (the
predictor column, or the grouping factor when the predictor does not vary); it
remains overridable. The layout follows from \code{x}: the remaining grouping
variables are always faceted, so the variable on the x-axis is never also used
as a facet. The plot style is derived, not an argument: a line with a
credible-interval ribbon is drawn only for a generated curve (the output of
\code{\link[=kb_predict_weight_by]{kb_predict_weight_by()}} over a varying predictor); every other prediction
renders as \code{geom_pointrange}. When the predictor does not vary (a held
reference value, or a model with no continuous predictor) the last grouping
factor goes on the x-axis. If the metadata has been stripped (e.g. by dplyr
post-processing) and \code{x} cannot be inferred, the function errors and asks for
\code{x}.
}
\examples{
# Allometric curve by site (ribbon):
kb_predict_weight_by(fit_weight_sim_nereo, by = "site") |>
  kb_plot_predictions()

# Weight at a reference diameter by site (pointrange, sites on the y-axis):
kb_predict_weight_by(
  fit_weight_sim_nereo,
  by = "site", diameter = 30, new_levels = "average"
) |>
  kb_plot_predictions() +
  coord_flip()
}
\seealso{
Other prediction:
\code{\link[=autoplot.kb_predictions]{autoplot.kb_predictions()}},
\code{\link[=kb_predict_weight]{kb_predict_weight()}},
\code{\link[=kb_predict_weight_by]{kb_predict_weight_by()}}
}
\concept{prediction}
