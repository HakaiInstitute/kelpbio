% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kb_fit_progress.R
\name{kb_fit_progress}
\alias{kb_fit_progress}
\title{Completed Fraction of a Running Fit}
\usage{
kb_fit_progress(progress_dir)
}
\arguments{
\item{progress_dir}{A string giving the directory passed as \code{progress_dir} to
a \verb{kb_fit_*} function.}
}
\value{
A number between 0 and 1.
}
\description{
The proportion of sampling completed by a fit writing to \code{progress_dir}, as a
number from 0 to 1.
}
\details{
Pass the same directory given as \code{progress_dir} to a \verb{kb_fit_*} function. This
reads fit progress from another R process: for example, a Shiny app can run
the fit in a background task and poll \code{kb_fit_progress()} from the main
session to drive a progress indicator. It returns \code{0} before sampling has
produced any output (or when the directory holds no fit artifact yet) and \code{1}
once every chain has finished.
}
\examples{
progress_dir <- tempfile()
dir.create(progress_dir)
# Before a fit has written anything, progress is 0.
kb_fit_progress(progress_dir)
}
